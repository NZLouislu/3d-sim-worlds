import CityScene from "@/components/city-simulation/CityScene";

export const metadata = {
  title: "City Simulation | 3D Sim Worlds",
  description: "Procedural city simulation with traffic system",
};

export default function CityPage() {
  return <CityScene />;
}
